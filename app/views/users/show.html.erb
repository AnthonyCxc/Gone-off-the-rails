<%#= provide(:title, "#{@user.name}'s Profile")%>

<div class="user-profile">
  <div class="row">
    <aside class="col-md-4">
      <section class="user_info">
        <h1>
          <%#= avatar_for @user %>
          <%= @user.name %>
        </h1>
        <%= @user.email %>
      </section>
    </aside>
  </div>
<p>TEST</p>